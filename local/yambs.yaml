---
includes:
  - ../config/python/yambs/cflag_groups.yaml
  - package://yambs/includes/wasm.yaml

default_target: wasm

variants:
  debug: &debug
    enabled: false
  opt: *debug
  clang: *debug
  clang-opt: *debug
